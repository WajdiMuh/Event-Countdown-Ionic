import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wajdi.eventcountdown',
  appName: 'Event Countdown',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
